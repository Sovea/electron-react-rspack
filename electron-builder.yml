appId: com.example.my-electron-app
productName: electron-app
copyright: Copyright © 2025 ${author}
asar: false
directories:
  output: build
  buildResources: dist
files:
  "dist/**/*"
win:
  icon: assets/rspack.png
  target:
    - target: msi
      arch:
        - x64
msi:
  artifactName: ${name}-${version}-setup.${ext}
  perMachine: true
  createDesktopShortcut: true